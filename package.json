{
  "name": "paynowdemo",
  "version": "1.0.1",
  "description": "Paynow SDK Demo",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/ --out-file dist/index.js --source-maps --presets=es2015,flow"
  },
  "license": "MIT",
  "dependencies": {
    "js-sha512": "^0.7.1",
    "request": "^2.87.0",
    "request-promise-native": "^1.0.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-flow": "^6.23.0"
  },
  "babel": {
    "presets": [
      "flow",
      "es2015"
    ]
  }
}
